# URL de l'API Laravel utilisée pendant le build (sitemap + prerender)
VITE_API_URL=https://admin.spa-prestige-collection.com

# URL publique du front (utilisée par sitemap.xml)
VITE_FRONT_URL=https://spa-prestige-collection.com

# Activé par "npm run build:prerender" — laisser à 0 pour un build normal
# PRERENDER=1
